# Copy this file to .env and fill in ONE of these (not required to run the app —
# without a key you still get retrieval + citations, just an extractive answer
# instead of an LLM-synthesized one).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
